
function oposto(number) {
  //para retornar o oposto do numero passado como argumento eu multiplico esse numero por 2 e subtraio esse valor pelo numero do argumento, isso fará com que o retorno seja o número negativo do que foi passado como argumento.
  number = number - number * 2;
  return number;
}
